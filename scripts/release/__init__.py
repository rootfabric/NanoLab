"""NanoLab release tooling (stdlib-only).

No third-party dependencies are allowed here: hosted CI runs the unittest
gate on a bare Python installation ("Check 5/5 ... stdlib only" in
.github/workflows/hosted-ci.yml).
"""
