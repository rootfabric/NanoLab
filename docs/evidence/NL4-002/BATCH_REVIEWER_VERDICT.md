# BATCH REVIEWER VERDICT — NL4-002 / кампания E3

- **Executions (батч)**: `EX-NL4-002-E3-MECH-R1` (arms random + grid, merge `64b9163`), `EX-NL4-002-E3-LLM-R1` (arm llm, merge `8a3156a`)
- **Точка ревью**: worktree `review/nl4-002-e3-r1`, exact HEAD `8a3156a37df1aee2f55e342d31cb9a5d2bb277a3` (merged main = origin)
- **Дата**: 2026-09-13 (fresh REVIEWER-сессия)
- **Independance caveat**: REVIEWER работает в fresh-сессии, не участвовавшей в implementer-руках; доверие — только файлам Git и воспроизводимым пересчётам (unittest, work_cli, check-consistency, независимый пересчёт median из raw trajectories в WSL). Хронология изолированности LLM-решений проверена по git-коммитам и событиям, но абсолютная достоверность машинных timestamps принимается по внутренним консистентным доказательствам (git author/commit times + event-файлы), а не по внешнему аудиту часов.

## Таблица обязательных проверок (1–10)

| # | Проверка | Результат | Детали |
|---|----------|-----------|--------|
| 1 | Пререгистрация WO до arms | **PASS** | `592c4a1` (2026-09-13 13:28+10) — предок обеих веток (`merge-base --is-ancestor` ok для `bce5fa7` и `db0af45`). Design-параметры (target 90°, budget 5×50000, seed-тройка 201004/202008/203012, frozen гейты E2_PROTO_R1 §4, winner-criterion, re-validation seeds 204016/205020) подтверждены во всех run-артефактах: goal-блоки ledgers обоих arms, `input.in` каждого рана (steps=50000, print_conf_interval=4000), манифесты указывают frozen arm-manifest-32b |
| 2 | Тесты | **PASS с finding F-1** | `python -m unittest discover -s tests -t .` → **Ran 287 tests, OK (skipped=1)**. Заявленные implementer'ами «291» НЕ воспроизводятся: 5 тестов `tests/test_nl4_real_executor.py` — module-level функции без `unittest.TestCase`, unittest их не коллектит (см. F-1) |
| 3 | Счётчики бюджетов | **PASS** | MECH: random 5/5 runs (ledger `E3-RANDOM-arm.json`: runs_executed=5, scored=5, invalid=0, rejected=0), grid 5/5 (`E3-GRID-arm.json`), smoke вне бюджета arm'ов. LLM: ledger `llm-runs-ledger.json` 5 ранов, все ANALYSED, exit 0; `llm-arm-session.json` canonical replay: simulations_used=5, proposals 5/5 accepted, 0 rejected. Решения до наблюдений: strategy в START-коммите `db0af45` (06:05Z), per-iteration rationale в `llm-decisions.json`, события iter1–iter5 07:04–09:41Z |
| 4 | Пересчёт ключевых чисел из raw | **PASS** | Независимый пересчёт median из WSL raw (`/home/yurig/nl4-002-e3/runs/…`, frozen observables v2 + arm-manifest-32b): E3-RANDOM-R004 → **78.3226448488771°** (заявлено 78.322644849; Δ < 1e-9); E3-LLM-R003 → **80.1679980715557°** (заявлено 80.167998072; Δ < 1e-9). Score 11.6774 / 9.8320 воспроизведены точно. sha256 trajectories: RANDOM-R004 `4022ccef…53de` = манифест; LLM-R003 `3edf3a5d…993f` = манифест. Frames 12/12 валидны в обоих. Расхождений > 0.01° нет |
| 5 | Честность сравнения | **PASS** | Бюджет симуляций равен (5×50000 у всех трёх агентов); allowlist равен (одинаковое runner-level сужение {0b,11b,32b,53b}×50k, 74b BLOCKED — честно, `allowlist.json` не менялся); движок/параметры ранов идентичны (pinned oxDNA 00dc7fb9, verbatim pro_CPU.in, одинаковые print-интервалы, проверено diff-инспекцией input.in). Wall-cap breach — только mech (9/10, 4280–4566 с при капе 4320 с; 11b медленнее и в random: 4387 с vs 4323 с у 0b — подтверждено ledger'ом), природа breach — 10-параллельный контеншн, все раны полные 50k, exit 0. LLM arm последовательный (2320–2760 с, без breach). Wall не входит в observables → физический скор не смещён; условия задокументированы симметрично для Director |
| 6 | Инцидент изоляции | **VALID** (см. отдельную секцию) | Хронология подтверждена; зависимости решений от утечки нет |
| 7 | Стерильность | **PASS** | `git diff 592c4a1..8a3156a --name-only` — только `docs/work/**`, `scripts/nl4/**`, `tests/**`; `project/state.json` не менялся (diff пуст); байтов источника (top/conf) в Git не добавлено (ls-files: только legacy E0/E1-артефакты, существовавшие до 592c4a1); U4: временные исходники удалены (заявлено в обоих summaries, run-каталоги WSL содержат только верифицированные копии); интерпретаций тренда нет (claim ceiling C0_SOFTWARE_ONLY соблюдён) |
| 8 | Валидаторы | **PASS** | `work_cli validate` → ok (оба), `work_cli close` → ok (оба, HANDOFF_READY, terminal handoff present, summary.md present); `PYTHONPATH=scripts python -m harness.cli check-consistency --root .` → ok: true, errors: [] |
| 9 | Winner-определение | **PASS** | min score по all-runs ledger: LLM R003 32b/203012 = 9.832 < RANDOM R004 11.677 < GRID R004 16.232. Tie нет. Winner candidate = 32b/203012. Re-validation план (свежие seeds 204016/205020 вне allowlist-тройки) следует напрямую из WO-NL4-002 §Winner-критерий и обоснован пилотной вариативностью 32b при 50k (77.04–80.17°, меж-seed разброс ~3.1°) — см. предупреждение о регрессии к среднему ниже |
| 10 | Records/бюджеты | **PASS** | Wall-цифры согласованы (ledger ↔ summary ↔ E3-MECH-summary.json: breach 5 random + 4 grid = 9/10, все с полными 50k); крэш-инцидентов движка нет (exit 0 × 15); инцидент первого рана LLM раскрыт и корректен: background-job погиб на границе turn'а ДО старта движка, run-каталог не создавался (в WSL ровно E3-LLM-R001..R005, никаких лишних каталогов), бюджет не списан — пере-запуск легитимен по WO («счётчик тратится на ЗАПУЩЕННЫЙ ран»; движок не запускался) |

## Per-arm вердикты

### EX-NL4-002-E3-MECH-R1 (random + grid)
**PASS.** Real ExecutorAdapter + smoke + оба механических arm'а воспроизводимы: 10/10 ранов exit 0, полные 50000 шагов, 12/12 кадров валидны, invalid/failed/rejected = 0. Пересчёт лучшего рана (random 32b/201004) из raw совпал до 1e-9°. Wall-cap breach 9/10 честно задокументирован (контеншн 10 параллельных ранов, +до 5.7% сверх капа, без потери данных), природа breach идентична для обоих arm'ов; interrupt-path gap раскрыт и починен пост-фактум без пересчёта данных. Отклонение «манифесты уже в base» и runner-level сужение пространства раскрыты в deviations/summary. Claim ceiling C0_SOFTWARE_ONLY соблюден.

### EX-NL4-002-E3-LLM-R1 (llm)
**PASS.** Стратегия пререгистрирована до первого рана (db0af45, 06:05Z), все 5 итераций последовательны, rationale каждого решения записан до наблюдения; budget 5/5; гейты PASS во всех ранах; пересчёт лучшего рана (32b/203012, 80.168°) из raw совпал до 1e-9°; sha256 манифестов сходятся. Отклонение run-root (`/home/yurig/nl4-002-e3/runs/` вместо планового `-llm`) раскрыто в summary, префиксы E3-LLM уникальны, смешения нет (F-3). Инцидент изоляции раскрыт добровольно, после фиксации всех решений — см. ниже.

## Isolation incident assessment

**Факты (проверено по Git):**
1. LLM-ветка создана от `64b9163` (merge mech-ветки). `git show 64b9163:docs/work/SESSION_LOG.md` подтверждает: base-коммит УЖЕ содержал сводный entry EX-NL4-002-E3-MECH-R1 с агрегатами (random best 32b/201004 11.68; grid best 11b/201004 16.23). SESSION_LOG — обязательный allowed-файл для аппенда; утечка произошла через хвост файла при append, не через чтение каталога `EX-NL4-002-E3-MECH-R1/**`.
2. Хронология: strategy зафиксирована коммитом `db0af45` (2026-09-13T06:05:17Z); события итераций 1–5 — 07:04Z … 09:41Z; инцидент ~10:05Z (handoff 10:05Z). Все 5 решений (включая финальный порядок 32b×3 → 11b×2) зафиксированы ДО инцидента timestamps'ами событий и коммитами (`bf8528a` 18:13+10 = 08:13Z для итераций 1–3; `9eb20c7` 19:51+10 = 09:51Z для 4–5).

**Зависимость решений от увиденных агрегатов:**
- Выбор LLM (32b×3 seeds, потом 11b×2) побайтово соответствует preregistered стратегии из db0af45, записанной ДО любого доступа к mech-результатам.
- Стратегия выводима исключительно из опубликованных E2-чисел (32b 78.09° — ближайший к 90°; между 32b и 53b нет промежуточных вариантов → 32b доминирует при любом знаке 50k-сдвига) — это подтверждается самим текстом preregistered rationale.
- Увиденные агрегаты не могли улучшить знание LLM сверх уже доступного: random best 32b (11.68) лишь подтверждает E2-порядок; более того, благодаря детерминизму движка идентичные кандидаты дают идентичные результаты (LLM-R001 32b/201004 ≡ RANDOM-R004 байт-в-байт по анализу), т.е. «пользу» от знания random-результатов невозможно реализовать — ран той же конфигурации дал бы тот же исход.

**Вердикт: comparison VALID.** Изоляция формально нарушена (мех-агрегаты попали в поле зрения LLM-сессии), но ПОСЛЕ фиксации всех пяти решений, и содержание утечки было информационно подчинено опубликованным E2-данным. Влияние на выбор кандидатов и на пост-фактум-рассуждение исключено ранней фиксацией (llm-decisions.json записан до инцидента). Для будущих кампаний рекомендован процесс-фикс (F-4): не вливать сводки arm'ов в shared SESSION_LOG до закрытия последнего arm'а, либо dispatch'ить LLM-arm от коммита до mech-merge.

## Findings

| ID | Severity | Finding | Action |
|----|----------|---------|--------|
| F-1 | MINOR (claim/verification gap) | «unittest 291 OK (1 skip)» из summary обоих executions НЕ воспроизводим: фактический `python -m unittest discover -s tests -t .` = **287 OK (1 skip)**. Причина: 5 тестов `tests/test_nl4_real_executor.py` объявлены module-level функциями без `unittest.TestCase` — unittest их не коллекционирует (grep находит 292 `def test_`, коллектится 287). Тесты «существуют, но не бегут» | Обернуть в `class TestRealExecutor(unittest.TestCase)`; не переоценивать suite-числа в records |
| F-2 | INFO | Wall-cap breach 9/10 (mech) — контеншн 10 параллельных ранов; все раны полные, exit 0; breach не влияет на физические observables (wall не входит в скор), условия обоих mech-arms идентичны; LLM arm последовательный, breach-free. Задокументировано симметрично | Director принимает/оговорит; для будущих кампаний калибровать кап под параллельность |
| F-3 | INFO | Run-root LLM arm: `/home/yurig/nl4-002-e3/runs/` вместо планового `/home/yurig/nl4-002-e3-llm/runs/` (frozen real_executor). Раскрыто в summary; префикс `E3-LLM-` уникален, смешения с mech-ранами нет (проверено листингом WSL) | Принять как задокументированное отклонение |
| F-4 | PROCESS (рекомендация) | Инцидент изоляции возможен из-за mech-сводки в shared SESSION_LOG в base-коммите LLM-ветки | В будущих multi-arm кампаниях: не мержить arm-сводки в SESSION_LOG до закрытия всех arm'ов, либо branching LLM-arm от pre-merge SHA |
| F-5 | INFO | Timestamps событий LLM — минутной точности, без Z-суффикса (naive); парсятся, но грубее git-коммитов. Хронология независимо подтверждена commit-times | Не блокирует; для новых событий использовать полную ISO-8601 UTC-точность |

Ни один finding не влияет на научные числа, бюджетный учёт или winner-определение.

## Общий вердикт

# PASS

Кампания E3 воспроизводима и честна: пререгистрация соблюдена, бюджеты равны и подтверждены ledger'ами, ключевые числа лучшего рана каждого arm'а пересчитаны из raw с точностью < 1e-9°, sha256 артефактов сходятся, валидаторы зелёные, стерильность соблюдена, инцидент изоляции корректно оценён как не повлиявший на сравнение (VALID). F-1 (мёртвые тесты при завышенном claim) — единственное расхождение с records; оно процессное, не научное, и лечится точечным фиксом без пересчёта данных.

## Winner recommendation для Director

- **Winner candidate: 32b / seed 203012 (E3-LLM-R003), score 9.832** (|80.168 − 90|); победитель по критерию — LLM arm. Δ(best baseline − LLM) = 11.677 − 9.832 = **1.845°** в пользу LLM arm.
- **Оговорка n=5/best-of**: разница 1.85° сопоставима с меж-seed разбросом 32b при 50k (77.04–80.17, ~3.1°) — «вклад ИИ» на этой задаче статистически хрупок; claim ceiling C0/measured-only, обобщения запрещены WO.
- **Re-validation (обязательно по WO)**: 32b × свежие seeds 204016/205020. **Предупреждение о регрессии к среднему**: 80.17° — лучший из четырёх наблюдений 32b@50k (77.04/78.32/80.17 + E2-медиана 78.09@150k); ожидание для свежих seeds — ближе к ~78°, т.е. score скорее сместится к ~11–12, чем останется 9.83. По WO выход за пределы аннулирует заявленный скор — это ожидаемое поведение anti-selection-bias гейта, а не провал кампании; Director следует заранее зафиксировать интерпретацию (аннулируется только claimed score кандидата, не вывод E3 о стратегиях).
- Re-validation seeds вне allowlist-тройки — санкционировано WO (Director, пост-кампания, verification-шаг).
