# NL3-001 — VERIFIER VERDICT (независимая верификация evidence-пакета EX-NL3-001-R1)

**Вердикт: `PASS`.**

- **Subject:** ветка `work/nl3-001-hinge-family-r1` @ exact HEAD `d01ff0332848caaa1f687314dd3e5d9c04be1935`; base `5fbd6d54341947c890cc5aa5f8031de10a0de1eb` — предок (`git merge-base --is-ancestor` → exit 0). Review: `review/nl3-001-hinge-family-r1` @ `84f71e7` (REVIEWER PASS, errata F-1/F-2 + info F-3/F-4/F-5) — принята к сведению, все её факты перепроверены независимо (см. ниже); настоящая верификация опирается только на факты Git и собственные прогоны.
- **Верификатор:** независимый fresh-агент (VERIFIER), worktree `C:\NanoLab\verify-nl3-001`, ветка `verify/nl3-001-hinge-family-r1`; машинный журнал — [VERIFIER_VERIFICATION_LOG_R1.md](VERIFIER_VERIFICATION_LOG_R1.md). Окружение: Python 3.11.8, git 2.53.0.windows.1, Windows. Network — только бесплатное публичное чтение pinned-объектов GitHub (API tree + raw по exact commit, ~2.7 MB, разрешённый режим источника); скачанные байты — disposable scratch вне репозиториев NanoLab, durable-кэш не создавался, published-поверхности не изменялись.
- **Claim ceiling:** не затронут — `C0_SOFTWARE_ONLY`; научные прогоны не выполнялись, campaign-level `scientific_outcome = NOT_EVALUATED`, кампания E2 = `NOT_RUN`. Настоящий вердикт не является accept'ом NL3-001 и не повышает claim; merge — Human Gate.

## 1. Результаты по пунктам верификации

| # | Пункт | Результат |
|---|---|---|
| 1 | Scope/ancestry | **PASS** — base предок subject; diff `5fbd6d5..d01ff03` = ровно 23 файла, +2660/−0, все в allowed_paths, по-коммитно чисто (START `01bef0b` до substantive; `08585a2` pins+validator+tests; `80aed84` регистрация; `d01ff03` records). `project/state.json` (b3fe19db…) и `project/plan.json` (c3e854da…) blob-идентичны base; `experiments/evidence/**`, published-research, harness-конфиг/скрипты, workflows — не тронуты; SESSION_LOG — строго append (+10). Вендоренных байтов источника нет: 0 строк формата `.top`/`.conf`/`pro_CPU.in` в добавленных строках; в Git только дайджесты/размеры/структурные факты и уже опубликованные на base значения параметров (INPUT_AVAILABILITY, E2-SETUP-R1 §5) |
| 2 | source_pins | **PASS** — ровно 18 поверхностей. Все 6 `NL0_001_PREREGISTERED` blob'ов (5 × `Design_Hinges/*.json` + `pro_CPU.in`) совпадают с `INPUT_AVAILABILITY.md` на base бит-в-бит (blob+размер). Все 18 blob SHA-1 + размеры сверены с GitHub API tree `b2d6ceb…` (`truncated=false`, 33 entries) — **0 mismatches**. SHA-256: ровно 5 поверхностей `0b` = `R1_CONTENT_VERIFIED` — все 5 воспроизведены собственным user-side download (size+SHA-256+blob OK); остальные 13 честно `sha256: null` + `NOT_VERIFIED`, контракт `pins.py` enforcement'ит «null ⇔ NOT_VERIFIED» в обе стороны |
| 3 | **КЛЮЧЕВОЕ: воспроизведение §4–§5** | **PASS — ПОЛНОСТЬЮ ВОСПРОИЗВЕДЕНО (не NOT_OBSERVED).** Pinned-объекты получены тем же разрешённым способом (raw.githubusercontent.com по exact commit `23fd1ff…`, scratch вне репозиториев): 5 поверхностей `0b` — digest-гейт PASS (size+SHA-256+blob, включая оба preregistered NL0-001 blob'а бит-в-бит) → `python -m hinge_family validate` **exit 0 ×2, 8/8 PASS**; два прогона байт-идентичны, **SHA-256 = `F8EF3EA4F9BA6CE5C9995894BF43743E853E9C582A596ABE9EE121C592D449A5` — ровно заявленный**, и **байт-в-байт равен git-blob'у published-отчёта** `hinge-0b-structural-report.json` @ `d01ff03` (проверка по `git cat-file`; worktree-копия `14DFA702…` = CRLF-артефакт `autocrlf=true` — F-4 подтверждён) |
| 4 | Арифметика 8378==8378 + инварианты | **PASS** — 34/34 инвариантов пересчитаны из собственного воспроизведённого отчёта: A2081+C2076+G2075+T2146=8378; Σ длин 112 страндов = 8378 (max 644/min 18); 103 linear + 9 circular (ids 2–7, 42–44); 8275 = 8378 − 103; Σ гистограммы = 8275; длинных (>0.95) 456 = Σ(buckets ≥ 1.0); **scaffold 4266 + staples 4112 = 8378 == 8378 нуклеотидов** (check PASS); 117 staple-путей Σ=4112; 2×4110+158=8378; ssDNA 16×2+6×9+6×12=158; кроссоверы 46+413=459; пути 1+117=118; `t=20000000`==`steps=2e7`; ориентации 2.15e-07 ≤ 1e-06; бокс 402.56³ |
| 5 | Тесты | **PASS** — `tests.test_hinge_family`: **22/22 OK**; полный набор `unittest discover`: **160/160 OK** (138 + 22) |
| 6 | E2-SETUP-R1 §5 machine-confirm | **PASS** — на реальных байтах `pro_CPU.in`: `DNA2 / salt 0.5 / T=300K / steps 2e7 / CPU / double` + `seed 7777, thermostat john, dt 0.005, rcut 2.0, external_forces 0, verlet_skin 0.05, refresh_vel 1` (= OBSERVED §3.2); опубликованный `PREREGISTERED`-набор == §5 published E2-SETUP-R1 (base, не менялся). Дефолт `74b.top/74b.conf` подтверждён, подстановка `74b`→`0b` зафиксирована как обязательное документируемое отклонение. Независимая FAIL-проба: `T=310K` (scratch-копия) → SimInputError (FAIL как ожидается); немодифицированный контроль → PASS |
| 7 | Harness-чеки (регрессия) | **PASS** — `work_cli validate`: **15/15 EX OK** (EX-NL3-001-R1: ok, HANDOFF_READY, терминал последний, post-terminal corrections нет); `check-consistency` ok, exit 0; JSON-гейт: 721 tracked @ `d01ff03` (= 717 @ `80aed84` + 4 records-JSON, как в event 0003), unparseable ровно 2 designed-NEG (sha256 = CI-пинам `e3b0c442…`, `1de18ae4…`); `workflow lint` 0 violations; `verify-digests` E1-R1 (4 runs/16 entries) / E1-R2 / E0-R4 — ok=true ×3, 0 mismatch |
| 8 | События/паспорт; self-acceptance | **PASS** — события 0001→0004 (STARTED → CONTINUATION → VALIDATION → HANDOFF), все IMPLEMENTER, метки монотонны, терминал последний; `tree(80aed84)` = `f437c3ae…` == заявке event 0003. Self-acceptance отсутствует: `ACCEPTED` не выставлен ни в одной новой поверхности, паспорт `HANDOFF_READY`, state/plan не тронуты, merge — Human Gate |

## 2. Errata review-вердикта (84f71e7) — независимо подтверждены

- **F-1 (LOW) — подтверждено:** published-артефакт `hinge-0b-structural-report.json`, bucket `"0.5"` = **4677** (максимум гистограммы; подтверждён и в моём независимом воспроизведении — отчёт байт-идентичен), тогда как `HINGE_FAMILY_R1.md` §5 пишет «4657 из 8275». Гистограмма внутренне сходится (Σ=8275) — ошибка перепечатки в prose. Ни один PASS-check на этом числе не стоит. Закрыть errata до Director checkpoint; пины и отчёт (заморожены) не трогать.
- **F-2 (LOW) — подтверждено по tree-listing:** полный pinned tree (GitHub API, `truncated=false`, 33 entries) содержит 9 путей вне «полного перечёта» §2, включая `Design_Hinges/README.md` (268 B, blob `08986156…`) и конкретное содержимое `Init_Hinges/` (README.md 570 B, `cadnano_interface.py` 56 325 B, `init_generator.py` 2906 B, `ini_demo/*`). На реестр не влияет: «18 зарегистрированных поверхностей» — их ровно 18, все верифицированы. Скорректировать формулировку там же, где F-1.
- **F-3 (INFO) — подтверждено по git:** `d01ff03` добавляет также event 0002 и evidence-отчёт помимо перечисленного в event 0004 (717+4=721 JSON сходится); целостности не вредит.
- **F-4 (INFO) — подтверждено:** worktree-копия отчёта = `14DFA702…` (CRLF), git-blob = `F8EF3EA4…` (LF, == воспроизведённому); проверка по `git cat-file` обязательна.
- **F-5 (INFO) — подтверждено чтением тестов:** позитивный путь digest-гейта для пина с `sha256: null` не покрыт (фикстуры `pin_entry` везде с SHA-256); `test_report_contains_no_wall_clock_fields` — слабая substring-проверка. Не блокирует; кандидат в pre-E2 инструментальную итерацию.

## 3. Новые находки верификации

- **V-1 (LOW, точность durable-записи; в класс errata F-1):** `HINGE_FAMILY_R1.md` §5 «вторая полоса 1.2–1.7 (**≈448**)» — строго по артефакту сумма buckets 1.2–1.7 = **440** (19+41+112+159+87+22); значение 448 соответствует buckets ≥ 1.1 (включая bucket `"1.1"` = 8). Из-за знака «≈» и того, что ни один check на этом не стоит — не блокирует; рекомендуется объединить с errata F-1/F-2 одной правкой prose до Director checkpoint.

## 4. Заключение

Все существенные утверждения пакета EX-NL3-001-R1 независимо воспроизведены: ключевое — **полное воспроизведение структурного отчёта `0b` 8/8 PASS байт-в-байт (`F8EF3EA4…D449A5`) из первоисточника тем же разрешённым digest-gated способом**; 18/18 пинов совпали с GitHub tree; арифметика 8378==8378 и 34 инварианта сходятся; 22/22 и 160/160 тестов зелёные; полный harness-набор (15/15 EX, consistency, JSON-гейт, lint, verify-digests) зелёный; режим REFERENCE_ONLY соблюдён (ни одного байта источника в Git, durable-кэш не создавался). Errata F-1/F-2 (+V-1) закрыть docs-правкой до Director checkpoint — на вердикт и claim ceiling не влияют.

**Вердикт: `PASS`.** Следующее действие — Director checkpoint NL3-001 (после errata F-1/F-2/V-1); merge — Human Gate.
