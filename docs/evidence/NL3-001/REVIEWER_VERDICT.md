# NL3-001 — REVIEWER VERDICT (независимый review evidence-пакета EX-NL3-001-R1)

**Вердикт: `PASS`** (все существенные утверждения пакета независимо воспроизведены, включая полное байт-в-байт воспроизведение структурного отчёта `0b` из первоисточника; найденные замечания — LOW/INFO, на вердикт и claim ceiling не влияют, корректировка — errata в follow-up, см. F-1/F-2).

- **Subject:** ветка `work/nl3-001-hinge-family-r1` @ exact HEAD `d01ff0332848caaa1f687314dd3e5d9c04be1935`; base `5fbd6d54341947c890cc5aa5f8031de10a0de1eb` (ancestor подтверждён: `git merge-base` = base).
- **Рецензент:** независимый fresh-агент (REVIEWER), без доступа к контексту имплементёра; только Git-факты и собственные прогоны. Review-ветка `review/nl3-001-hinge-family-r1`, worktree `C:\NanoLab\review-nl3-001`.
- **Окружение:** Python 3.11.8, Windows; прогоны — disposable scratch (`%TEMP%`); published-поверхности не изменялись. Network-использование — только бесплатное публичное чтение pinned-объектов GitHub (~2.7 MB, разрешённый режим источника, WO §Resource budget); скачанные байты удалены после проверки, durable-кэш не создавался (политика U4 соблюдена рецензентом).
- **Claim ceiling:** не затронут — `C0_SOFTWARE_ONLY`; научные прогоны не выполнялись, campaign-level scientific_outcome = `NOT_EVALUATED`; кампания E2 = `NOT_RUN`. Настоящий вердикт не является accept'ом NL3-001 и не повышает claim.

## 1. Резюме по bounded scope

| # | Пункт | Результат |
|---|---|---|
| 1 | Scope diff `5fbd6d5..d01ff03` = allowed_paths; state/plan не тронуты; вендоренных байтов источника нет | **PASS** — 23 файла, 0 violations: `docs/work/WO-NL3-001.md`, `EX-NL3-001-R1/**`, `SESSION_LOG.md` (строго append, +10 строк), `HINGE_FAMILY_R1.md`, `scripts/hinge_family/**` (11 файлов), `tests/test_hinge_family.py`. `project/state.json`/`plan.json`, `experiments/evidence/**`, published-research, config/harness, workflows — не тронуты. По-коммитно чисто: START `01bef0b` (WO+паспорт+event 0001) до substantive; `08585a2` pins+validator+tests; `80aed84` регистрация; `d01ff03` records. Вендоринг: в добавленных строках нет ни одной строки тела `.top` (шаблон `<sid> <base> <n3> <n5>`), ни одной 15-колоночной строки `.conf`, ни байтов `pro_CPU.in` (933 B); публикуются только дайджесты/размеры/производные структурные факты и уже опубликованные в E2-SETUP-R1 §5 имена/значения параметров; test-фикстура `SIM_INPUT` — синтетическая (иной набор ключей, нет `topology`/`conf_file`), не байты источника. Pinned-объекты вне Git — подтверждено (`git ls-files` + `--stat`) |
| 2 | `source_pins.json`: реестр 18 поверхностей; blob SHA-1 vs NL0-001 provenance; SHA-256 пяти 0b-поверхностей воспроизведены; NOT_VERIFIED честно | **PASS** — ровно 18 файлов. Все 6 `NL0_001_PREREGISTERED` blob'ов (`Design_Hinges/{0b,11b,32b,53b,74b}.json`, `pro_CPU.in`) + размеры совпадают с `INPUT_AVAILABILITY.md` (NL0-001) бит-в-бит. Все 18 blob SHA-1 + размеры независимо сверены с GitHub API tree `b2d6ceb…` (`truncated=false`, 33 entries, **0 mismatches**) — это одновременно подтверждает claim «полный API-перечёт, не truncated» (§1). SHA-256 пяти `R1_CONTENT_VERIFIED` поверхностей воспроизведены моим собственным user-side download (size+SHA-256+blob — все совпали). Остальные 13 — `sha256: null` + `NOT_VERIFIED`; контракт `pins.py` enforce'ит «null ⇔ NOT_VERIFIED» в обе стороны |
| 3 | Воспроизведение 8/8 §4–§5 + 22 unittest | **PASS — полное независимое воспроизведение.** User-side download пяти поверхностей `0b` по exact pinned commit → digest-гейт PASS → `python -m hinge_family validate` **exit 0 ×2**, 8/8 PASS; два прогона байт-идентичны, SHA-256 = `F8EF3EA4F9BA6CE5C9995894BF43743E853E9C582A596ABE9EE121C592D449A5` — **ровно заявленный** в HINGE_FAMILY_R1 §5 и summary, и **байт-идентично git-blob'у** опубликованного `hinge-0b-structural-report.json` @ `d01ff03` (см. F-4 про worktree/CRLF). `tests.test_hinge_family` — 22/22 OK; полный набор `unittest discover` — **160 OK** (138 + 22). OBSERVED-значения §3.2 (`seed=7777`, `thermostat=john`, `dt=0.005`, `rcut=2.0`, `external_forces=0`, `verlet_skin=0.05`, `refresh_vel=1`, `topology=74b.top`, `conf_file=74b.conf`) подтверждены на реальных байтах |
| 4 | Кросс-чек 8378=8378 и всей арифметики published-артефактов | **PASS** — 60/60 инвариантов пересчитаны скриптом из опубликованного отчёта: состав A2081+C2076+G2075+T2146=8378; 112 длин странд Σ=8378 (max 644, min 18); 103 linear + 9 circular = 112 (ids 2–7, 42–44); identity связанных дистанций **8275 = 8378 − 103**; гистограмма Σ=8275; длинных (>0.95) 456 = Σ(buckets ≥ 1.0); scaffold 4266 + staples 4112 = **8378 == 8378 нуклеотидов**; 117 staple-путей, Σ длин = 4112; 2×4110 paired + 158 single = 8378; ssDNA-серии 16×2+6×9+6×12 = 158; кроссоверы 46+413 = 459; пути 1+117 = 118; `t=20000000` == `steps=2e7`; ориентации 2.15e-07 ≤ 1e-06; box 402.56³ |
| 5 | UNKNOWN-дисциплина: U1–U6, классы REPORTED/OBSERVED/UNKNOWN | **PASS** — U1/U4 = E2-SETUP-R1 §10 Q1/Q5 (владелец, с NL0-002), U2 = Q2 (SI, pre-E2), U5 = Q4 (compatibility audit), U3/U6 — новые, из честных R1-наблюдений, с местом решения; ничего не выдумано. REPORTED (spring layers 0/24…74/84, oxDNA2, 298 K, 500 mM, average-base) трассируется в NL0-001/E2-SETUP; OBSERVED — всё machine-confirm'нуто рецензентом на pinned-байтах; `salt 0.5 ↔ 500 mM` честно помечен ASSUMED с путем подтверждения; угол/пороги не назначены. Ничего не выдумано |
| 6 | G1–G6 полнота; скрытые риски прав/вендоринга | **PASS** — G1 права (владелец), G2 дизайн→топология 118 vs 112, G3 атрибуция spring layers, G4 семантика колонок 9:15, G5 SHA-256 `11b–74b`, G6 динамика NL3-002. Неприкрытых рисков класса прав/вендоринга не найдено: LICENSE_POLICY/NL0-002 (REFERENCE_ONLY, «UNKNOWN ≠ разрешение») соблюдены; в Git — только дайджесты/факты (прецедент INPUT_AVAILABILITY); durable-кэш отсутствует. Подмена путей `74b→0b` явно зафиксирована как обязательное документируемое отклонение |
| 7 | E2-SETUP-R1 §5 machine-confirm vs decision rule | **PASS** — `sim_input.py PREREGISTERED` = OBSERVED-набор E2-SETUP §5 точно (DNA2/0.5/300K/2e7/CPU/double); reproduction arm = авторские файлы verbatim (300 K), расхождение 298 K документируется, не «исправляется»; paper-fidelity arm — только отдельная revision; проверка T=310K → FAIL (unittest). Дефолт `74b.top/74b.conf` в `pro_CPU.in` подтверждён и зафиксирован как наблюдение с обязательной явной подстановкой |
| 8 | Тесты 22: качество, негативы, детерминизм | **PASS** — позитив на синтетическом консистентном наборе; детерминизм in-process + через CLI (байтовое сравнение + свойство канонической сериализации); тампер-негативы: изменённый байт → digest FAIL (`sha256_ok=False` asserted), усечение → size FAIL, отсутствующий файл → read_error; структурные негативы: base letter, n3/n5, row count, NaN, 14 колонок, non-unit orientation, particle count, pointer consistency, design↔topology count, T=310K; контракт реестра: malformed blob sha, `mode != REFERENCE_ONLY` → PinsError; целостность bundled-пинов (NL0-001 blob'ы не изменены, verified-набор ровно 5, роли вариантов). Замечания — F-5 |
| 9 | События/паспорт/схемы; self-acceptance | **PASS** — `work_cli validate` EX-NL3-001-R1: ok, HANDOFF_READY, терминал 0004 последний, post-terminal corrections нет; все 15 EX-каталогов валидны; `check-consistency` ok exit 0; JSON-гейт: 721 tracked @ `d01ff03` (= 717 @ `80aed84` ровно как в event 0003, + 4 файла records-коммита), unparseable ровно 2 designed-NEG с sha256 = CI-пинам (`e3b0c442…`, `1de18ae4…`). Self-acceptance отсутствует: ACCEPTED нигде не выставляется, route REVIEWER → VERIFIER → Director, merge — Human Gate |

## 2. Findings

### F-1 (LOW, точность durable-записи; рекомендована errata): «главный пик 0.5–0.6 (**4657** из 8275)» противоречит published-артефакту — фактически 4677

`HINGE_FAMILY_R1.md` §5 (строка «Распределение связанных дистанций») пишет 4657; в авторитетном артефакте — `hinge-0b-structural-report.json`, bucket `"0.5"` = **4677** (подтверждено и в моём независимом воспроизведении: отчёты байт-идентичны). Гистограмма внутренне сходится (Σ=8275), отчёт верен — это ошибка перепечатки в prose-строке документа (4677 → 4657). Ни один PASS-check, факт или вывод не опирается на это число. Исправить errata-строкой/правкой в follow-up docs-коммите до Director checkpoint; пины и отчёт не трогать (заморожены).

### F-2 (LOW, точность durable-записи; рекомендована errata): «OBSERVED-состав pinned tree (полный перечёт, R1)» — перечень не исчерпывающий

§2 `HINGE_FAMILY_R1.md` перечисляет 8 позиций; полный tree (GitHub API, `truncated=false`, 33 entries) дополнительно содержит `Design_Hinges/README.md` (268 B) и конкретное содержимое `Init_Hinges/` (README.md, `cadnano_interface.py` 56 KB, `init_generator.py`, `ini_demo/*`) — последнее частично покрыто упоминанием каталога, но `Design_Hinges/README.md` в «полном перечёте» отсутствует. На дайджесты и реестр не влияет (реестр заявляет ровно «18 зарегистрированных поверхностей» — их ровно 18 и все верифицированы). Скорректировать формулировку («ключевой состав»; полный перечень — API tree SHA) там же, где F-1.

### F-3 (INFO, records bookkeeping): event 0004 описывает содержимое records-коммита не полностью

`0004-handoff-completed.json` утверждает «настоящий records-коммит добавляет только события 0003–0004, summary, паспорт-статус и SESSION_LOG append»; фактически `d01ff03` добавляет также `0002-continuation-checkpoint.json` и `evidence/hinge-0b-structural-report.json` (717+4=721 JSON — сходится). Timestamps событий попарно упорядочены и содержательно event 0002 соответствует контенту `08585a2`/`80aed84`; раскрытие «tip публикуется push'ом» (урок F5 NL2-001) соблюдено. Целостности не вредит; принять к сведению.

### F-4 (INFO, эргономика верификации): `core.autocrlf=true` — SHA-256 worktree-копии отчёта ≠ опубликованному

Канонические байты в Git (LF) = `F8EF3EA4…` = воспроизведённому отчёту; рабочая копия после checkout нормализуется в CRLF (`14DFA702…`). Проверку детерминизма/воспроизводимости выполнять по `git cat-file blob` (или при `autocrlf=false`), о чём полезно явно сказать в инструкции VERIFIER'а (сам пакет пишет `newline="\n"` — корректно).

### F-5 (INFO, покрытие тестов): два узких пробела, не блокирующих

(а) позитивный путь digest-гейта для пина с `sha256: null` (`NOT_VERIFIED`, проход по size+blob) тестом не покрыт — синтетические фикстуры везде с SHA-256; (б) `test_report_contains_no_wall_clock_fields` — слабая substring-проверка. Рекомендация — добавить в pre-E2 инструментальную итерацию, не в ремонт R1.

## 3. Self-acceptance

Не обнаружен: `ACCEPTED` в новых поверхностях отсутствует (только дисклеймеры «ACCEPTED не выставляется»); паспорт `HANDOFF_READY`; событие 0004 терминальное, следующих за ним нет; next action — независимый REVIEWER (настоящий вердикт), затем VERIFIER и Director checkpoint; merge — Human Gate. `project/state.json`/`plan.json` не изменялись — объявление статуса NL3-001 остаётся за Director.

## 4. Заключение

Регистрация семейства Shi–Castro–Arya выполнена в заявленном bounded scope и в режиме REFERENCE_ONLY без единого байта источника в Git. Ключевые научные гарантии пакета независимо подтверждены рецензентом на реальных pinned-объектах: preregistered blob'ы NL0-001 сошлись бит-в-бит; 8/8 структурных проверок воспроизведены; отчёт воспроизведён **байт-в-байт** (`F8EF3EA4…`), что закрывает и требование детерминизма; кросс-чек 8378=8378 и вся арифметика published-артефактов сходятся; UNKNOWN/REPORTED/OBSERVED-дисциплина и гэпы G1–G6 честны; механический контроль (`pins.py` fail-closed, 22 unittest, 160 OK, 15/15 EX, JSON-гейт) зелёный. F-1/F-2 закрыть errata до Director checkpoint. Вердикт не повышает claim и не является accept'ом.

**Next action:** VERIFIER — независимая перепроверка вердикта (минимум: git-blob SHA-256 отчёта = `F8EF3EA4F9BA6CE5C9995894BF43743E853E9C582A596ABE9EE121C592D449A5`, реестр vs `INPUT_AVAILABILITY.md`, 160 unittest, errata F-1/F-2), затем Director checkpoint.
