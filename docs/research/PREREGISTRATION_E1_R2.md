# E1-PROTO-R2 — Пререгистрация E1 (superseding R1 только в части §6.4: freeze R_confirm)

Идентификатор протокола: `E1-PROTO-R2`. Статус: **PREREGISTERED (freeze R_confirm зафиксирован ДО confirmatory кампании)**. Supersedes: `E1-PROTO-R1` исключительно в пункте §6.4 (назначение `R_confirm`). Все остальные пункты R1 (observables, критерий §5.1, условия §3, семантика §9, stop conditions §10) наследуются без изменений и не пересматриваются. Основание: `E1-PROTO-R1` §6.4 (процедура пилота и момент freeze). Work Order: `NL1-002`. Дата freeze: 2026-09-09.

## 1. Пилот (факты, §6.4.1–2)

Pilot: ровно 3 независимых прогона `E1-R1-P001..P003` (verbatim `quick_input`, seed — случайный engine; PILOT, в evidence не засчитываются). Фактические seed: −473348953, −547126645, −1610133928.

| Реплика | ColumnAverage(col2) | Δ от оракула | В полосе ±0.15 | Wall, s |
|---|---|---|---|---|
| P001 | −1.37730121179 | +0.00240134965 | да | 11.08 |
| P002 | −1.39389370430 | −0.01419114286 | да | 11.74 |
| P003 | −1.38687945155 | −0.00717689011 | да | 12.56 |

Между-репликационный разброс: среднее −1.38602478921, выборочное SD **0.00832919790** (5.6% полосы), размах 0.0166. Малое n — без претензии на точный CI (§6.3). Фактическая стоимость: ~11–13 c/прогон (1 ядро), RSS ~6.3–6.7 MB.

## 2. Freeze (§6.4.3)

```text
R_confirm = 3 (независимых production-прогонов, distinct seeds, criteria §5.1 на каждую)
```

Обоснование (письменно, до confirmatory кампании): между-репликационное SD пилота на два порядка меньше полосы ±0.15; все 3 пилот-реплики в полосе; стоимость 3 confirm-прогонов ≈ 40 c CPU — консервативно выше минимума §6.4.3 (`R_confirm ≥ 2`) и даёт NL2-002 осмысленное n для между-репликационной статистики. После старта confirmatory кампании `R_confirm` не уменьшается; полоса §5.1 не меняется (без новой superseding revision).

## 3. Правило confirmatory кампании (NL2-002)

T2 = 3 независимых прогона verbatim-конфигурации §3 R1 с фактически записанными distinct seed. Критерий на каждую реплику — §5.1 R1. Кампания E1 объявляется SUPPORTED только при T1 = PASS **и** все T2-реплики в полосе (консервативно, не слабее upstream); иначе — согласно §9 R1 (`NOT_SUPPORTED` сохраняется как результат). Повторное использование пилот-прогонов как T2 запрещено (§6.4: pilot не засчитывается в evidence).

## 4. Неизвестные, не закрытые этой версией

Точные CI между-репликационного разброса (n мал), корреляции/ESS внутри-траектории — диагностическая задача NL2-002 (PyMBAR-level инструменты), физическая валидность oxDNA — вне scope (S02 R1).
