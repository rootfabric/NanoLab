# E1 — Воспроизведение небольшого известного примера

Статус: **SUPPORTED** (научное объявление Director, 2026-09-10, первое объявление статуса эксперимента линии; [DIRECTOR_ACCEPTANCE_R1](../../evidence/NL2-002/DIRECTOR_ACCEPTANCE_R1.md)). Пререгистрированный критерий `E1-PROTO-R1` §9 (T1 PASS **и** все T2-реплики IN_BAND) выполнен **механически** на confirmatory set `S001 + C001–C003` (n = 4: mean −1.36896744830, выборочное SD 0.01729656703 = 11.5% полосы ±0.15; per-replica |Δ| = 0.0142/0.0125/0.0215/0.0232) и подтверждён независимо трижды: implementer (кампания `E1-R2` по frozen `E1-PROTO-R2`, R_confirm = 3, distinct seeds), Reviewer (бит-в-бит воспроизведение статистики из published-блобов, Decimal; 7/7 seeds distinct; `9e26748`), Verifier (fresh rebuild pinned oxDNA + собственная реплика C-VERIFY, seed 319832093 → −1.39396936364 IN_BAND; `fe24e48`). **Claim ceiling `C1_COMPUTATIONAL_REPRODUCTION`**: подтверждена независимая вычислительная репродукция upstream-эталона (`quick_compare` −1.37970256144 ± 0.15) на pinned среде; это НЕ утверждение о физической валидности oxDNA, сборке, кинетике или переносимости (S02). Ограничения: одна машина/сборка (NATIVE-привязка; независимость повторов = distinct seeds), SD на n = 4 грубая и вдвое выше пилотной (5.6% → 11.5% — пилотная оценка была оптимистичной), Neff ≈ 8–48 на траекторию с оценкой для C003 до ~7 (кадры не независимы; PyMBAR-level ESS открыт), seeds случайны (воспроизводима статистика, не точные значения), CRLF-ловушка Windows working-copy для дайджест-сверок. Execution-статус в `project/state.json` остаётся `E1 = RUN` по конвенции execution-facts. Evidence: [E1-R1](../../experiments/evidence/E1/E1-R1/campaign.md), [E1-R2](../../experiments/evidence/E1/E1-R2/campaign.md). Среда: [ENGINE_ENVIRONMENT_R1](../research/ENGINE_ENVIRONMENT_R1.md). Основные кандидаты источников: [S01–S03](../research/SOURCES.md).

## Вопрос

Можно ли воспроизвести заранее выбранное наблюдение небольшой ДНК-системы по доступному опубликованному или upstream-протоколу?

## Отбор объекта

Начать с небольшого дуплекса либо простой конструкции из проверенного примера. Наличие читаемой статьи недостаточно: нужны topology, configuration/способ получения, sequence/model assumptions, условия, настройки и reference observable. Выбор и права закрываются в NL0.

## Последовательность

Импорт и контроль входов → подготовка → уравновешивание → production → анализ → сравнение. Сначала короткий smoke, проверяющий интеграцию и стоимость; затем отдельно достаточная для выбранного вывода кампания.

## Наблюдения

Выбрать одно основное и небольшое число диагностических свойств из источника. Это может быть геометрическая характеристика или согласованное распределение, но не произвольная величина, выбранная после просмотра результата. Сохранить точное определение, область данных и метод оценки ошибки.

## Контроли и приёмка

Независимые повторы/исходные состояния по протоколу, проверка чувствительности численных настроек, сравнение распределений или оценок в пререгистрированных пределах. Отличать smoke PASS от принятого воспроизведения.

Расхождение не скрывается: проверить входы, определение наблюдения, версии, применимость и sampling. Недостаточная информация даёт INCONCLUSIVE/blocked source, а не выдуманный PASS. Смена эталона оформляется новым решением.

Артефакты: воспроизводимый пакет, версии, checksum, команды, ресурсы, анализ, сравнение и ограничения. Исходный эталон выбран (NL0-001: DSDNA8/MD) и численный критерий пререгистрирован до кампании: [E1-PROTO-R1](../research/PREREGISTRATION_E1_R1.md) — условия verbatim upstream `quick_input`, оракул `quick_compare` `-1.37970256144 ± 0.15`.
